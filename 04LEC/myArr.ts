const superHeros: string[] = []
// const heroPower: number[] = []

type User = {
    name: string;
    email: string;
    isActive: boolean;
}

const allUsers: User[] = []

allUsers.push({name: "Rajan", email: "nr@r", isActive: true})

console.log(allUsers)

const MLModels: number[][]=[
    [1,2,3],
    [4,5,6],
]

export {}